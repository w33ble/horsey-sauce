// runner.once('tunnel-ready', id => console.log('EVENT: tunnel created', id, timer.diff()));
// runner.on('browser-ready', () => console.log('EVENT: browser ready', timer.diff()));
// runner.on('runner-start', () => console.log('EVENT: running code on remote', timer.diff()));
// runner.on('runner-end', () => console.log('EVENT: running code complete', timer.diff()));
// runner.on('close', () => console.log('EVENT: tunnel closed', timer.diff()));
